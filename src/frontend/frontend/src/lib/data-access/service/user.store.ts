import {User} from "../models";
import {Injectable} from "@angular/core";
import {BackendService} from "./backend.service";

@Injectable({
  providedIn: 'root'
})

export class UserStore {

  constructor(private backendService: BackendService) {
  }

  postNewsletterSubscribtion():
}
